---
phase: 05-verification-and-extension-readiness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - src/lib/astro/adapter.ts
  - src/lib/engine/decisionEngine.ts
  - src/lib/engine/scoring.ts
  - src/app/api/today/[userId]/route.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - tests/astro/adapter.test.ts
  - tests/engine/scoring.test.ts
  - tests/api/validation.test.ts
  - tests/api/today-route.test.ts
sequential_files: []
expected_artifacts:
  - path: "README.md"
    provides: "Maintainer-facing local setup and verification guide"
    required: true
  - path: "src/lib/astro/adapter.ts"
    provides: "Visible seam for optional swisseph support and future transit-depth extension"
    required: false
  - path: "src/lib/engine/scoring.ts"
    provides: "Visible seam for future decision-category or scoring growth"
    required: false
autonomous: false
agents: [engineering-senior-developer, project-management-project-shepherd]
requirements: [R11, R12]
user_setup: []
verification_commands:
  - "test -f README.md"
  - \"grep -q 'npm install\\|npm run test:run\\|npm run dev' README.md\"
  - \"grep -q 'database-free\\|no database' README.md\"
  - \"grep -q 'swisseph\\|fallback' README.md\"
must_haves:
  truths:
    - "A new maintainer can set up, verify, and run the project locally from the README."
    - "The documentation remains honest about the optional swisseph seam and the current fallback path."
    - "Deferred extension seams for persistence, richer transit logic, and additional decision categories are easy to find."
    - "Phase 5 ends with a clean local-first handoff instead of an implicit maintainer-only understanding."
  artifacts:
    - path: "README.md"
      provides: "Local run and verification guide"
      min_lines: 40
      contains: "Kaal|npm install|npm run test:run|npm run dev|database|swisseph"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "documented local commands match the actual scripts"
      pattern: "dev|test:run|typecheck"
    - from: "README.md"
      to: "src/lib/astro/adapter.ts"
      via: "optional runtime seam is documented honestly for local environments"
      pattern: "swisseph|fallback"
    - from: "README.md"
      to: "src/app/api/today/[userId]/route.ts"
      via: "persistence is presented as a deferred seam rather than an existing feature"
      pattern: "today|stateless|persistence"
---

<objective>
Document the local run path and make extension readiness obvious so Kaal can be handed off cleanly after the verification matrix is locked.

Purpose: turn the verified backend into a maintainer-friendly, local-first v1 handoff grounded in a proven bootstrap and verification path.
Output: a real README and explicit extension-seam guidance aligned to the actual codebase and the proven Phase 5 verification contract.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/05-verification-and-extension-readiness-spec.md
@.planning/phases/05-verification-and-extension-readiness/05-CONTEXT.md
@.planning/phases/05-verification-and-extension-readiness/05-01-SUMMARY.md
@.planning/phases/04-api-delivery-layer/04-REVIEW.md

Relevant source files:
- package.json
- src/lib/astro/adapter.ts
- src/lib/engine/decisionEngine.ts
- src/lib/engine/scoring.ts
- src/app/api/today/[userId]/route.ts

Agent: engineering-senior-developer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a real maintainer-facing README</name>
  <files>README.md</files>
  <action>
Create a root README that explains what Kaal is, how to install dependencies, how to run the verification suite, how to start the local backend, and what local-first constraints define v1. The README should be enough for a new maintainer to get productive without reading the planning files first.

Treat the README as the output of a proven bootstrap exercise from Wave 1, not as an aspirational document. Keep the setup path aligned to the actual scripts in `package.json`, call out any expected fallback behavior, and be explicit that v1 is database-free.

> verification: test -f README.md
> verification: grep -q 'npm install\|npm run test:run\|npm run dev' README.md
> verification: grep -q 'database-free\|no database' README.md
  </action>
  <verify>
test -f README.md
grep -q 'npm install\|npm run test:run\|npm run dev' README.md
grep -q 'database-free\|no database' README.md
  </verify>
  <done>The project has a real local setup and run guide that a new maintainer can follow.</done>
</task>

<task type="auto">
  <name>Task 2: Add concrete extension-seam artifacts instead of cosmetic notes</name>
  <files>src/lib/astro/adapter.ts, src/lib/engine/decisionEngine.ts, src/lib/engine/scoring.ts, src/app/api/today/[userId]/route.ts, README.md</files>
  <action>
Make the future seams for richer transit logic, persistence, and additional decision categories explicit and discoverable through concrete handoff artifacts. Targeted code-adjacent comments, invariants, or short boundary notes are allowed, but they must point to the exact places where those future capabilities belong without reverse-engineering the architecture.

Do not implement those future capabilities now. Clarify the seams without expanding scope, and avoid generic comments that could be pasted anywhere.

> verification: grep -q 'swisseph\|fallback' README.md
> verification: grep -q 'persistence\|stateless' README.md
  </action>
  <verify>
grep -q 'swisseph\|fallback' README.md
grep -q 'persistence\|stateless' README.md
  </verify>
  <done>The codebase clearly signals where future transit depth, persistence, and decision growth belong.</done>
</task>

<task type="auto">
  <name>Task 3: Align the final handoff story to proven local reality</name>
  <files>README.md</files>
  <action>
Use the verification outputs from Wave 1 to make the final README and handoff framing honest about what is proven locally in this environment. The documentation should describe the current no-database setup, the test and typecheck commands, the canonical local verification command set, and the runtime assumptions without overselling capabilities that are only deferred seams.

This task should leave the repo in a state where final review can judge readiness directly from the code and README together, including what was actually proven versus what remains intentionally deferred.

> verification: grep -q 'typecheck\|test:run\|dev' README.md
> verification: grep -q 'extension\|future' README.md
  </action>
  <verify>
grep -q 'typecheck\|test:run\|dev' README.md
grep -q 'extension\|future' README.md
  </verify>
  <done>The final handoff documentation matches what the codebase actually proves today and what it intentionally defers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The README documents install, verification, and local run commands.
- [ ] The README reflects a proven local verification and bootstrap path from Wave 1.
- [ ] The no-database v1 constraint is explicit.
- [ ] The optional swisseph seam and fallback reality are documented honestly.
- [ ] Extension seams for persistence, richer transits, and new decision categories are easy to identify through concrete handoff artifacts.
</verification>

<success_criteria>
- A new maintainer can run Kaal locally without planning-file archaeology.
- The handoff is local-first, honest, and extension-ready.
- Phase 5 ends with documentation and seam notes that reflect proven behavior instead of aspirations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-verification-and-extension-readiness/05-02-SUMMARY.md`

Summarize the local setup guide, the proven runtime assumptions, the canonical verification path, and the explicit extension-seam artifacts that now make Kaal ready for future growth without redesign.
</output>
