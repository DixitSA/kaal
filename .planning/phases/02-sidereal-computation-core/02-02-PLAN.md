---
phase: 02-sidereal-computation-core
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/astro/calculateDasha.ts
  - src/lib/astro/calculateTransits.ts
  - src/lib/astro/calculateTaraBala.ts
  - tests/astro/calculateDasha.test.ts
  - tests/astro/calculateTaraBala.test.ts
  - tests/astro/calculateTransits.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - package.json
  - src/lib/astro/adapter.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/types/astrology.ts
  - src/lib/schemas/input.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/astro/calculateDasha.ts"
    provides: "Typed Vimshottari dasha state computation"
    required: true
  - path: "src/lib/astro/calculateTaraBala.ts"
    provides: "Tara bala classification and score helper"
    required: true
  - path: "src/lib/astro/calculateTransits.ts"
    provides: "Simple Moon-based transit modifier calculations"
    required: true
  - path: "tests/astro/calculateDasha.test.ts"
    provides: "Timing-state verification coverage"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-qa-verification-specialist]
requirements: [R2, R3]
user_setup: []
verification_commands:
  - "test -f src/lib/astro/calculateDasha.ts"
  - "test -f src/lib/astro/calculateTaraBala.ts"
  - "test -f src/lib/astro/calculateTransits.ts"
  - "test -f tests/astro/calculateDasha.test.ts"
  - "grep -q 'mahadasha' src/lib/astro/calculateDasha.ts"
must_haves:
  truths:
    - "Dasha, tara bala, and transit state logic exist as deterministic reusable modules rather than chart-inline side effects."
    - "Time-unknown handling is explicit and confidence-aware instead of crashing or silently pretending the birth time is known."
    - "Transit logic stays intentionally simple and Moon-based for v1 rather than drifting into a full classical transit engine."
    - "Dasha computation consumes helper or adapter-produced Moon inputs and never calls swisseph directly."
  artifacts:
    - path: "src/lib/astro/calculateDasha.ts"
      provides: "Typed dasha state computation"
      min_lines: 15
      contains: "mahadasha"
    - path: "src/lib/astro/calculateTaraBala.ts"
      provides: "Tara bala helper implementation"
      min_lines: 10
      contains: "tara"
    - path: "src/lib/astro/calculateTransits.ts"
      provides: "Moon-based transit modifiers"
      min_lines: 12
      contains: "moon"
    - path: "tests/astro/calculateDasha.test.ts"
      provides: "Dasha verification"
      min_lines: 12
      contains: "describe"
  key_links:
    - from: "src/lib/astro/calculateDasha.ts"
      to: "src/lib/astro/adapter.ts"
      via: "timing-state computation is fed by the isolated provider seam rather than direct library imports"
      pattern: "adapter|swisseph"
    - from: "src/lib/astro/calculateTransits.ts"
      to: "src/lib/astro/calculateTaraBala.ts"
      via: "daily-state inputs are composed from the same Moon-based primitives"
      pattern: "tara|moon"
---

<objective>
Implement the timing and daily-state astro modules that derive Vimshottari state, tara bala, and simple Moon-based transit modifiers with deterministic, confidence-aware behavior.

Purpose: establish the reusable timing and daily-state computations before final chart assembly.
Output: dasha, tara bala, and transit modules plus direct verification tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sidereal-computation-core/02-CONTEXT.md
@.planning/phases/02-sidereal-computation-core/02-01-SUMMARY.md

Relevant source files:
- src/lib/astro/calculateDasha.ts
- src/lib/astro/calculateTaraBala.ts
- src/lib/astro/calculateTransits.ts
- src/lib/types/astrology.ts
- src/lib/astro/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement typed Vimshottari dasha state calculation</name>
  <files>src/lib/astro/calculateDasha.ts</files>
  <action>
Replace the placeholder dasha module with a typed deterministic implementation that returns the mahadasha and antardasha state shape needed by the rest of the backend. Use the shared astro constants and keep the function pure. The goal for this phase is correctness and extension readiness, not interpretive text.

Model the current implementation so it can safely support exact-time and time-unknown paths. If some precision is degraded when the birth time is unknown, make that explicit in the return path or calling conventions rather than crashing or masking uncertainty.
Freeze the ownership boundary in the implementation notes: `calculateDasha.ts` must consume Moon birth primitives passed in from the adapter or chart-helper layer, and must not import or call `swisseph` directly.

> verification: test -f src/lib/astro/calculateDasha.ts
> verification: grep -q 'mahadasha' src/lib/astro/calculateDasha.ts
  </action>
  <verify>
test -f src/lib/astro/calculateDasha.ts
grep -q 'mahadasha' src/lib/astro/calculateDasha.ts
  </verify>
  <done>The dasha module returns typed Vimshottari timing state suitable for the rest of the chart pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tara bala and simple Moon-based transit modifiers</name>
  <files>src/lib/astro/calculateTaraBala.ts, src/lib/astro/calculateTransits.ts</files>
  <action>
Implement the v1 daily-state primitives: tara bala classification and simple Moon-based transit modifiers. Keep the transit logic intentionally compact and deterministic, focusing on the specific support/pressure/instability inputs described in the project brief rather than drifting into broader interpretive logic.

The output should be reusable by later daily-state engine work and safe to call in both exact-time and time-unknown flows.

> verification: test -f src/lib/astro/calculateTaraBala.ts
> verification: test -f src/lib/astro/calculateTransits.ts
> verification: grep -q 'tara' src/lib/astro/calculateTaraBala.ts
  </action>
  <verify>
test -f src/lib/astro/calculateTaraBala.ts
test -f src/lib/astro/calculateTransits.ts
grep -q 'tara' src/lib/astro/calculateTaraBala.ts
  </verify>
  <done>The daily-state input modules produce deterministic tara and Moon-based transit data for later engine work.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused timing and daily-state tests</name>
  <files>tests/astro/calculateDasha.test.ts, tests/astro/calculateTaraBala.test.ts, tests/astro/calculateTransits.test.ts</files>
  <action>
Add direct tests for the timing and daily-state primitives. At minimum, verify dasha output shape, tara bala classification or score mapping, and that the transit module returns the expected deterministic modifier structure. Include one confidence-aware or time-unknown edge case if the implementation surfaces that distinction at this layer.

> verification: test -f tests/astro/calculateDasha.test.ts
> verification: test -f tests/astro/calculateTaraBala.test.ts
> verification: test -f tests/astro/calculateTransits.test.ts
> verification: grep -q 'describe' tests/astro/calculateTransits.test.ts
  </action>
  <verify>
test -f tests/astro/calculateDasha.test.ts
test -f tests/astro/calculateTaraBala.test.ts
test -f tests/astro/calculateTransits.test.ts
grep -q 'describe' tests/astro/calculateTransits.test.ts
  </verify>
  <done>The timing and daily-state modules are protected by direct deterministic tests before chart integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dasha, tara bala, and transit modules exist as reusable deterministic astro functions.
- [ ] Dasha input ownership is explicit: the module consumes upstream Moon primitives and does not reach through the adapter boundary.
- [ ] Time-unknown or degraded-confidence behavior is handled explicitly where relevant.
- [ ] The timing and daily-state helper layer has direct tests.
</verification>

<success_criteria>
- Phase 2 has independent timing and daily-state primitives instead of burying them in chart assembly.
- Later identity, phase, and daily engines can consume stable astro-state outputs without reshaping this layer.
- Time-unknown handling is visible and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidereal-computation-core/02-02-SUMMARY.md`

Summarize the timing/daily-state modules, note any confidence assumptions, and record the tests that now protect them.
</output>
