---
phase: 02-sidereal-computation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/astro/adapter.ts
  - src/lib/astro/constants.ts
  - src/lib/astro/calculateJulianDay.ts
  - src/lib/astro/calculateNakshatra.ts
  - tests/astro/adapter.test.ts
  - tests/astro/calculateJulianDay.test.ts
  - tests/astro/calculateNakshatra.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/app/api/chart/compute/route.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/astro/calculateDasha.ts
  - src/lib/astro/calculateTransits.ts
  - src/lib/astro/calculateTaraBala.ts
  - src/lib/types/astrology.ts
  - src/lib/schemas/input.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/astro/adapter.ts"
    provides: "Real provider seam that contains all swisseph interactions behind a typed adapter"
    required: true
  - path: "src/lib/astro/calculateJulianDay.ts"
    provides: "Reusable Julian day calculation for chart assembly"
    required: true
  - path: "src/lib/astro/calculateNakshatra.ts"
    provides: "Reusable sign, nakshatra, and pada derivation helpers"
    required: true
  - path: "tests/astro/adapter.test.ts"
    provides: "Adapter-level runtime behavior coverage for normalized provider calls and error handling"
    required: true
  - path: "tests/astro/calculateNakshatra.test.ts"
    provides: "Helper-level astro tests for sign and nakshatra derivation"
    required: true
autonomous: false
agents: [engineering-backend-architect, testing-qa-verification-specialist]
requirements: [R1, R3]
user_setup: []
verification_commands:
  - "test -f src/lib/astro/adapter.ts"
  - "test -f src/lib/astro/calculateJulianDay.ts"
  - "test -f src/lib/astro/calculateNakshatra.ts"
  - "test -f tests/astro/adapter.test.ts"
  - "test -f tests/astro/calculateNakshatra.test.ts"
  - "grep -q 'swisseph' src/lib/astro/adapter.ts"
must_haves:
  truths:
    - "All ephemeris-provider interactions are isolated to src/lib/astro/adapter.ts and do not leak into other astro modules."
    - "The helper layer establishes deterministic sign, nakshatra, and pada derivation that later waves can reuse without duplicating longitude math."
    - "Lahiri remains the explicit default ayanamsha in the adapter/constants layer."
    - "The adapter boundary is proven by direct runtime-oriented tests rather than only by file existence or string checks."
  artifacts:
    - path: "src/lib/astro/adapter.ts"
      provides: "Typed swisseph adapter seam"
      min_lines: 20
      contains: "swisseph"
    - path: "src/lib/astro/calculateJulianDay.ts"
      provides: "Julian day helper implementation"
      min_lines: 10
      contains: "calculateJulianDay"
    - path: "src/lib/astro/calculateNakshatra.ts"
      provides: "Nakshatra and pada helper implementation"
      min_lines: 15
      contains: "calculateNakshatra"
    - path: "tests/astro/adapter.test.ts"
      provides: "Adapter behavior verification"
      min_lines: 12
      contains: "describe"
    - path: "tests/astro/calculateNakshatra.test.ts"
      provides: "Astro helper verification"
      min_lines: 12
      contains: "describe"
  key_links:
    - from: "src/lib/astro/adapter.ts"
      to: "src/lib/astro/calculateJulianDay.ts"
      via: "the provider seam consumes reusable helper math instead of embedding date logic inline"
      pattern: "calculateJulianDay"
    - from: "src/lib/astro/calculateNakshatra.ts"
      to: "src/lib/astro/constants.ts"
      via: "helper math reuses named sidereal constants instead of magic numbers"
      pattern: "NAKSHATRA|RASHI|AYANAMSHA"
---

<objective>
Replace the placeholder ephemeris seam with a real isolated adapter and implement the low-level sidereal helper math that all later chart computation depends on.

Purpose: establish the provider boundary and the reusable astro primitives before timing and chart assembly build on top of them.
Output: an isolated `swisseph` adapter, core helper modules, and focused helper tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sidereal-computation-core/02-CONTEXT.md
@.planning/phases/01-project-skeleton-and-contracts/01-REVIEW.md

Relevant source files:
- src/lib/astro/adapter.ts
- src/lib/astro/constants.ts
- src/lib/astro/calculateJulianDay.ts
- src/lib/astro/calculateNakshatra.ts
- src/lib/types/astrology.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the real provider seam and dependency boundary</name>
  <files>package.json, src/lib/astro/adapter.ts</files>
  <action>
Add the `swisseph` dependency to the project and replace the placeholder adapter with a real typed seam that contains all library-facing interactions. The adapter should establish the conventions the rest of the astro layer will rely on: Lahiri as the default ayanamsha, typed birth input handling, and a clear API for obtaining the sidereal longitudes or chart primitives that later waves will compose.

Do not scatter `swisseph` imports or option flags into the other astro modules. Any provider-specific setup, option normalization, or error translation belongs in `src/lib/astro/adapter.ts` only.
Record any install-time or runtime assumptions that the dependency introduces, such as ephemeris data access, path configuration, or platform-specific setup, so later waves do not rediscover them blindly.

> verification: test -f src/lib/astro/adapter.ts
> verification: grep -q 'swisseph' src/lib/astro/adapter.ts
> verification: grep -q 'swisseph' package.json
  </action>
  <verify>
test -f src/lib/astro/adapter.ts
grep -q 'swisseph' src/lib/astro/adapter.ts
grep -q 'swisseph' package.json
  </verify>
  <done>The project has a real `swisseph` dependency and all provider-facing logic is isolated to the astrology adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Julian day, sign, nakshatra, and pada helpers</name>
  <files>src/lib/astro/constants.ts, src/lib/astro/calculateJulianDay.ts, src/lib/astro/calculateNakshatra.ts</files>
  <action>
Implement the reusable low-level helpers that later chart assembly will depend on. This includes Julian day calculation and the sidereal longitude-to-sign/nakshatra/pada conversions using named constants rather than inline magic numbers. Make the helpers pure and typed so the chart pipeline can reuse them without route or provider leakage.

Use `src/lib/astro/constants.ts` to house the stable sidereal metadata needed for this work, including the rashi list, nakshatra list, spans, and any related derived constants that make the helper implementations easier to read and maintain.

> verification: test -f src/lib/astro/calculateJulianDay.ts
> verification: test -f src/lib/astro/calculateNakshatra.ts
> verification: grep -q 'calculateJulianDay' src/lib/astro/calculateJulianDay.ts
> verification: grep -q 'calculateNakshatra' src/lib/astro/calculateNakshatra.ts
  </action>
  <verify>
test -f src/lib/astro/calculateJulianDay.ts
test -f src/lib/astro/calculateNakshatra.ts
grep -q 'calculateJulianDay' src/lib/astro/calculateJulianDay.ts
grep -q 'calculateNakshatra' src/lib/astro/calculateNakshatra.ts
  </verify>
  <done>The low-level astro helper math is implemented as pure, named, reusable modules.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused adapter and helper tests</name>
  <files>tests/astro/adapter.test.ts, tests/astro/calculateJulianDay.test.ts, tests/astro/calculateNakshatra.test.ts</files>
  <action>
Add direct tests that verify both the adapter seam and the mathematical primitives before higher-level chart assembly depends on them. At minimum, include one adapter-level test that exercises a real normalized provider call shape and one explicit adapter error-handling path, plus helper coverage for Julian day output shape or determinism, longitude-to-sign conversion, nakshatra mapping, and pada derivation. Keep the tests deterministic and local-first.

> verification: test -f tests/astro/adapter.test.ts
> verification: test -f tests/astro/calculateJulianDay.test.ts
> verification: test -f tests/astro/calculateNakshatra.test.ts
> verification: grep -q 'describe' tests/astro/calculateNakshatra.test.ts
  </action>
  <verify>
test -f tests/astro/adapter.test.ts
test -f tests/astro/calculateJulianDay.test.ts
test -f tests/astro/calculateNakshatra.test.ts
grep -q 'describe' tests/astro/calculateNakshatra.test.ts
  </verify>
  <done>The adapter seam and helper layer are protected by direct astro tests before later waves build on them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swisseph` is added and isolated to `src/lib/astro/adapter.ts`.
- [ ] Adapter behavior is tested through at least one real normalized provider call path and one explicit error path.
- [ ] Julian day, sign, nakshatra, and pada helpers exist as pure reusable modules.
- [ ] Focused helper tests verify the foundational astro math.
</verification>

<success_criteria>
- The project has a real ephemeris boundary instead of a placeholder seam.
- Later phases can build chart assembly on reusable sidereal helper math rather than inline calculations.
- Helper-level astro regressions are caught by tests before they leak into chart computation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidereal-computation-core/02-01-SUMMARY.md`

Summarize the adapter seam, helper modules, the adapter behavior tests now in place, and any provider setup assumptions that later waves must respect.
</output>
