---
phase: 02-sidereal-computation-core
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/astro/adapter.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/types/astrology.ts
  - src/lib/schemas/input.ts
  - tests/astro/calculateChart.test.ts
  - tests/api/validation.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - package.json
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/astro/calculateChart.ts"
    provides: "Integrated chart primitive computation for exact-time and time-unknown modes"
    required: true
  - path: "src/lib/types/astrology.ts"
    provides: "Aligned chart primitive types for the real Phase 2 output shape"
    required: true
  - path: "src/lib/schemas/input.ts"
    provides: "Aligned chart request/output schemas reflecting the real chart shape"
    required: true
  - path: "tests/astro/calculateChart.test.ts"
    provides: "Integration-level chart computation coverage"
    required: true
autonomous: false
agents: [engineering-backend-architect, testing-api-tester]
requirements: [R1, R2, R3]
user_setup: []
verification_commands:
  - "test -f src/lib/astro/calculateChart.ts"
  - "test -f src/lib/types/astrology.ts"
  - "test -f tests/astro/calculateChart.test.ts"
  - "grep -q 'time-unknown' src/lib/types/astrology.ts"
  - "grep -q 'ChartPrimitives' src/lib/types/astrology.ts"
must_haves:
  truths:
    - "The chart computation pipeline supports both exact-time and time-unknown modes without crashing."
    - "Shared astrology-facing types and schemas match the actual Phase 2 chart primitive output instead of placeholder shapes."
    - "The existing chart route can consume the real chart computation pipeline without inventing a new contract."
    - "Phase 2 explicitly reconciles the current placeholder chart contract with the product-spec target fields instead of preserving the index-heavy placeholder model by inertia."
  artifacts:
    - path: "src/lib/astro/calculateChart.ts"
      provides: "Integrated chart primitive pipeline"
      min_lines: 25
      contains: "calculateChart"
    - path: "src/lib/types/astrology.ts"
      provides: "Aligned chart primitive types"
      min_lines: 30
      contains: "ChartPrimitives"
    - path: "src/lib/schemas/input.ts"
      provides: "Aligned chart schema"
      min_lines: 40
      contains: "chartPrimitivesInputSchema"
    - path: "tests/astro/calculateChart.test.ts"
      provides: "Chart computation integration coverage"
      min_lines: 15
      contains: "describe"
  key_links:
    - from: "src/lib/astro/calculateChart.ts"
      to: "src/lib/types/astrology.ts"
      via: "chart assembly emits the shared typed primitive structure used across the backend"
      pattern: "ChartPrimitives|BirthInput"
    - from: "src/lib/schemas/input.ts"
      to: "tests/api/validation.test.ts"
      via: "the chart route success-path validation matches the aligned schema"
      pattern: "chartPrimitivesInputSchema|chartResponseSchema"
---

<objective>
Assemble the full chart primitive computation pipeline and align the shared types and schemas with the real sidereal output produced in Phase 2.

Purpose: finish Phase 2 by turning the helper modules into an integrated chart computation path that supports exact-time and time-unknown requests.
Output: chart assembly, aligned contracts, and integration-level chart verification.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-sidereal-computation-core/02-CONTEXT.md
@.planning/phases/02-sidereal-computation-core/02-01-SUMMARY.md
@.planning/phases/02-sidereal-computation-core/02-02-SUMMARY.md
@.planning/phases/01-project-skeleton-and-contracts/01-REVIEW.md

Relevant source files:
- src/lib/astro/adapter.ts
- src/lib/astro/calculateChart.ts
- src/lib/types/astrology.ts
- src/lib/schemas/input.ts
- src/app/api/chart/compute/route.ts
- tests/api/validation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate exact-time and time-unknown chart primitive computation</name>
  <files>src/lib/astro/adapter.ts, src/lib/astro/calculateChart.ts</files>
  <action>
Replace the placeholder chart assembly with the real integrated pipeline that composes the provider seam and the helper modules from the earlier waves. The output must support both exact-time and time-unknown inputs without crashing, and the implementation should make confidence-aware degradation explicit rather than pretending all chart features are equally available.

Keep the adapter boundary intact: chart assembly can orchestrate the provider data, but raw `swisseph` access still belongs only in the adapter.

> verification: test -f src/lib/astro/calculateChart.ts
> verification: grep -q 'calculateChart' src/lib/astro/calculateChart.ts
  </action>
  <verify>
test -f src/lib/astro/calculateChart.ts
grep -q 'calculateChart' src/lib/astro/calculateChart.ts
  </verify>
  <done>The chart pipeline computes integrated chart primitives for exact-time and time-unknown requests.</done>
</task>

<task type="auto">
  <name>Task 2: Align shared astrology types and schemas to the real chart shape</name>
  <files>src/lib/types/astrology.ts, src/lib/schemas/input.ts</files>
  <action>
Update the shared astrology-facing types and chart-related schemas so they reflect the real Phase 2 chart primitive output rather than the Phase 1 placeholder approximation. Keep the contract alignment honest and consistent with the review lesson from Phase 1: no hidden alternate shapes or schema drift.

Preserve the exact-time and time-unknown modeling explicitly in the types and schema layer, including whatever confidence or optional-field behavior the final chart output needs. Do this as an explicit reconciliation against the product spec target shape: Lahiri default, `julianDayUT`, Moon sign/degree/nakshatra/pada, optional Lagna fields for unknown-time mode, typed planet positions, and house output responsibility. If the final implementation intentionally diverges from any target field, the plan summary must call that out directly rather than silently carrying the placeholder structure forward.

> verification: test -f src/lib/types/astrology.ts
> verification: grep -q 'ChartPrimitives' src/lib/types/astrology.ts
> verification: grep -q 'time-unknown' src/lib/types/astrology.ts
> verification: grep -q 'chartPrimitivesInputSchema' src/lib/schemas/input.ts
  </action>
  <verify>
test -f src/lib/types/astrology.ts
grep -q 'ChartPrimitives' src/lib/types/astrology.ts
grep -q 'time-unknown' src/lib/types/astrology.ts
grep -q 'chartPrimitivesInputSchema' src/lib/schemas/input.ts
  </verify>
  <done>The shared types and schemas now match the actual chart primitive structure emitted by Phase 2.</done>
</task>

<task type="auto">
  <name>Task 3: Add chart integration tests and route-level success-path verification</name>
  <files>tests/astro/calculateChart.test.ts, tests/api/validation.test.ts</files>
  <action>
Add integration-level chart tests that prove the assembled chart pipeline works in both exact-time and time-unknown modes. Also tighten the chart route validation or success-path assertions as needed so the existing API surface proves it can consume the real chart output.

Use deterministic assertions that check the shape and critical fields of the computed chart output rather than brittle snapshot noise.

> verification: test -f tests/astro/calculateChart.test.ts
> verification: grep -q 'describe' tests/astro/calculateChart.test.ts
> verification: grep -q 'chart/compute' tests/api/validation.test.ts
  </action>
  <verify>
test -f tests/astro/calculateChart.test.ts
grep -q 'describe' tests/astro/calculateChart.test.ts
grep -q 'chart/compute' tests/api/validation.test.ts
  </verify>
  <done>The real chart pipeline is covered by integration tests and the chart route success path is still protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The chart pipeline computes real chart primitives for both exact-time and time-unknown modes.
- [ ] Shared chart types and schemas align with the actual output shape and explicitly reconcile any remaining differences from the product-spec target fields.
- [ ] House output responsibility is modeled intentionally for both exact-time and time-unknown modes.
- [ ] Integration tests protect chart assembly and the chart route success path.
</verification>

<success_criteria>
- Phase 2 leaves the backend with a real sidereal computation layer instead of placeholders.
- The chart route can already return real chart primitives through the established contract.
- Later interpretation-engine phases can consume stable chart outputs without reshaping this layer again.
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidereal-computation-core/02-03-SUMMARY.md`

Summarize the integrated chart pipeline, the exact-time versus time-unknown handling, the final contract reconciliation against the product spec, and the chosen house-output behavior.
</output>
