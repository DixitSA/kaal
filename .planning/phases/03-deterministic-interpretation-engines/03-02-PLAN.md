---
phase: 03-deterministic-interpretation-engines
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/content/archetypePhrases.ts
  - src/lib/content/phasePhrases.ts
  - src/lib/content/dailyPhrases.ts
  - src/lib/content/decisionPhrases.ts
  - src/lib/types/engine.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
  - tests/engine/phraseSelection.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - src/lib/schemas/output.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/content/archetypePhrases.ts"
    provides: "Expanded identity phrase-bank data aligned with the product response direction"
    required: true
  - path: "src/lib/content/decisionPhrases.ts"
    provides: "Decision phrase-bank data keyed cleanly to deterministic outcomes"
    required: true
  - path: "src/lib/types/engine.ts"
    provides: "Rendered engine output types aligned to the richer Phase 3 phrase-bank structure"
    required: true
  - path: "tests/engine/phraseSelection.test.ts"
    provides: "Deterministic phrase-selection verification"
    required: true
autonomous: false
agents: [product-technical-writer, engineering-senior-developer]
requirements: [R4, R5, R6, R7, R8]
user_setup: []
verification_commands:
  - "test -f src/lib/content/archetypePhrases.ts"
  - "test -f src/lib/content/decisionPhrases.ts"
  - "test -f src/lib/types/engine.ts"
  - "test -f tests/engine/phraseSelection.test.ts"
  - "grep -q 'guidance\\|summary\\|risk\\|core' src/lib/content/archetypePhrases.ts"
  - "grep -q 'summary\\|guidance\\|rationale\\|watchouts\\|supportAction\\|cautionAction' src/lib/types/engine.ts"
must_haves:
  truths:
    - "Phrase banks are declarative data and do not absorb interpretation logic."
    - "Phrase selection is keyed by internal state and deterministic seeds rather than generic freeform variation."
    - "User-facing copy stays short, modern, and free of astrology jargon."
    - "Richer phrase-bank structure is reconciled into the exported engine output contract during Phase 3 instead of being deferred invisibly to Phase 4."
  artifacts:
    - path: "src/lib/content/archetypePhrases.ts"
      provides: "Expanded identity phrases"
      min_lines: 20
      contains: "core|summary|pattern|watchouts"
    - path: "src/lib/content/dailyPhrases.ts"
      provides: "Daily-state phrases"
      min_lines: 15
      contains: "guidance|caution"
    - path: "tests/engine/phraseSelection.test.ts"
      provides: "Phrase determinism verification"
      min_lines: 12
      contains: "describe"
    - path: "src/lib/types/engine.ts"
      provides: "Rendered engine output contract alignment"
      min_lines: 55
      contains: "summary|guidance|rationale|watchouts|supportAction|cautionAction"
  key_links:
    - from: "src/lib/engine/identityEngine.ts"
      to: "src/lib/content/archetypePhrases.ts"
      via: "identity rendering consumes phrase-bank data keyed by internal state"
      pattern: "deterministicPick|phrase"
    - from: "src/lib/engine/decisionEngine.ts"
      to: "src/lib/content/decisionPhrases.ts"
      via: "decision guidance is rendered from structured phrase data rather than hardcoded strings"
      pattern: "guidance|rationale|phrase"
    - from: "src/lib/types/engine.ts"
      to: "src/lib/content/archetypePhrases.ts"
      via: "the public engine output contract stays aligned with the richer Phase 3 phrase-bank structure"
      pattern: "summary|guidance|rationale|watchouts|supportAction|cautionAction"
---

<objective>
Expand the phrase banks and deterministic renderer logic so Phase 3 can produce richer short-form outputs without mixing rules and phrasing.

Purpose: align copy rendering to the clean state pipeline and the product’s compact response style.
Output: expanded phrase-bank data, exported output-contract alignment, renderer alignment, and deterministic phrase-selection tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/03-deterministic-interpretation-engines-spec.md
@.planning/phases/03-deterministic-interpretation-engines/03-CONTEXT.md
@.planning/phases/03-deterministic-interpretation-engines/03-01-SUMMARY.md

Relevant source files:
- src/lib/content/archetypePhrases.ts
- src/lib/content/phasePhrases.ts
- src/lib/content/dailyPhrases.ts
- src/lib/content/decisionPhrases.ts
- src/lib/engine/identityEngine.ts
- src/lib/engine/phaseEngine.ts
- src/lib/engine/dailyEngine.ts
- src/lib/engine/decisionEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand phrase-bank structures toward the product response shape</name>
  <files>src/lib/content/archetypePhrases.ts, src/lib/content/phasePhrases.ts, src/lib/content/dailyPhrases.ts, src/lib/content/decisionPhrases.ts, src/lib/types/engine.ts</files>
  <action>
Expand the phrase-bank data so it can support the product direction captured in the Phase 3 spec. Identity phrases should move closer to the target structure of compact emotional/decision/pattern language. Phase, daily, and decision phrase banks should also become rich enough to support more specific deterministic rendering without introducing long-form output.

Keep the copy short, modern, and jargon-free. Do not let the phrase-bank files become logic-heavy.

As part of this task, align `src/lib/types/engine.ts` with the richer rendered field structure that Phase 3 intends to expose publicly. Make it explicit which phrase-bank-backed fields become part of the engine outputs now so Phase 4 can wire routes without another response-shape redesign.

> verification: test -f src/lib/content/archetypePhrases.ts
> verification: test -f src/lib/content/decisionPhrases.ts
> verification: test -f src/lib/types/engine.ts
  </action>
  <verify>
test -f src/lib/content/archetypePhrases.ts
test -f src/lib/content/decisionPhrases.ts
test -f src/lib/types/engine.ts
  </verify>
  <done>The phrase banks and exported output types can support compact product-style rendering instead of only the starter placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor renderer logic to consume internal state keys</name>
  <files>src/lib/engine/identityEngine.ts, src/lib/engine/phaseEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts</files>
  <action>
Refactor the engine render steps so deterministic phrase selection is driven by the internal state and named keys established in Wave 1, not generic hashes alone. Keep render logic thin: it should map state to phrase-bank entries and select stable copy, not re-derive interpretation rules.

Each renderer should make its state-to-output mapping legible enough that later API work can expose the richer fields without inferring hidden structure from phrase data.

> verification: grep -q 'deterministicPick' src/lib/engine/identityEngine.ts
> verification: grep -q 'deterministicPick' src/lib/engine/phaseEngine.ts
  </action>
  <verify>
grep -q 'deterministicPick' src/lib/engine/identityEngine.ts
grep -q 'deterministicPick' src/lib/engine/phaseEngine.ts
  </verify>
  <done>The engine renderers now consume stable state keys and phrase-bank data rather than mixing selection logic with interpretation.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic phrase-selection tests</name>
  <files>tests/engine/phraseSelection.test.ts</files>
  <action>
Add direct tests that prove phrase selection is deterministic for the same internal state and seed inputs, and that changed state inputs can produce different rendered outputs when intended. The tests should also guard against accidental randomization or timestamp drift.

These tests should cover each engine family, not just one generic phrase helper, so identity, phase, daily, and decision rendering all prove they are consuming internal state keys correctly.

> verification: test -f tests/engine/phraseSelection.test.ts
> verification: grep -q 'describe' tests/engine/phraseSelection.test.ts
  </action>
  <verify>
test -f tests/engine/phraseSelection.test.ts
grep -q 'describe' tests/engine/phraseSelection.test.ts
  </verify>
  <done>The phrase-rendering layer is protected against random drift and unstable selection behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Phrase banks are expanded enough to support compact product-style outputs.
- [ ] Exported engine output types are aligned to the richer rendered structure.
- [ ] Renderer logic consumes internal state keys rather than re-deriving rules.
- [ ] Phrase-selection behavior is directly tested for determinism.
</verification>

<success_criteria>
- Copy rendering is richer than the starter placeholders without becoming verbose.
- Deterministic selection remains stable for identical inputs.
- Later API work can expose better outputs without rewriting the engine architecture or re-inventing the output contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-interpretation-engines/03-02-SUMMARY.md`

Summarize the phrase-bank expansion, the output-contract alignment, the renderer alignment, and the deterministic selection tests that now protect the content layer.
</output>
