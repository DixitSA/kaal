---
phase: 03-deterministic-interpretation-engines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/engine.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
  - src/lib/engine/scoring.ts
  - tests/engine/identityEngine.test.ts
  - tests/engine/phaseEngine.test.ts
  - tests/engine/dailyEngine.test.ts
  - tests/engine/decisionEngine.test.ts
  - tests/engine/scoring.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - src/lib/content/archetypePhrases.ts
  - src/lib/content/phasePhrases.ts
  - src/lib/content/dailyPhrases.ts
  - src/lib/content/decisionPhrases.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/types/engine.ts"
    provides: "Expanded engine-facing types for internal state and rendered outputs"
    required: true
  - path: "src/lib/engine/scoring.ts"
    provides: "Named scoring constants, category weighting maps, and reusable interpretation helpers"
    required: true
  - path: "tests/engine/identityEngine.test.ts"
    provides: "Identity-state and mapping verification"
    required: true
  - path: "tests/engine/decisionEngine.test.ts"
    provides: "Decision threshold and category verification"
    required: true
  - path: "tests/engine/scoring.test.ts"
    provides: "Chart-derived weighting and normalized daily-seed verification"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-qa-verification-specialist]
requirements: [R4, R5, R6, R7]
user_setup: []
verification_commands:
  - "test -f src/lib/engine/scoring.ts"
  - "test -f src/lib/types/engine.ts"
  - "test -f tests/engine/identityEngine.test.ts"
  - "test -f tests/engine/decisionEngine.test.ts"
  - "test -f tests/engine/scoring.test.ts"
  - "grep -q 'type IdentityState\\|interface IdentityState' src/lib/types/engine.ts"
  - "grep -q 'career\\|relationships\\|money\\|travel\\|move\\|communication' src/lib/engine/scoring.ts"
  - "grep -q 'normalize\\|daySeed\\|dailySeed' src/lib/engine/scoring.ts"
must_haves:
  truths:
    - "Internal state derivation is separated from phrase rendering for identity, phase, daily, and decision logic."
    - "Named thresholds and scoring helpers replace inline interpretation constants."
    - "Decision scoring is chart-derived and category-specific rather than remaining hash-dominant behind renamed helpers."
    - "Time-unknown handling remains explicit in engine logic rather than being silently flattened."
    - "Daily and date-aware rendering foundations use a shared normalized day-seed rule rather than raw timestamp entropy."
  artifacts:
    - path: "src/lib/types/engine.ts"
      provides: "Internal engine state types"
      min_lines: 55
      contains: "IdentityState|PhaseState|DailySignalState|DecisionState|IdentityProfile|PhaseProfile|DailyState|DecisionEvaluation"
    - path: "src/lib/engine/scoring.ts"
      provides: "Reusable interpretation scoring helpers and category weighting maps"
      min_lines: 40
      contains: "career|relationships|money|travel|move|communication|threshold|score|modifier|normalize"
    - path: "tests/engine/decisionEngine.test.ts"
      provides: "Decision logic verification"
      min_lines: 12
      contains: "describe"
    - path: "tests/engine/scoring.test.ts"
      provides: "Scoring and daily-seed verification"
      min_lines: 12
      contains: "describe"
  key_links:
    - from: "src/lib/engine/identityEngine.ts"
      to: "src/lib/types/engine.ts"
      via: "identity derivation emits a typed internal state before phrase rendering"
      pattern: "IdentityState"
    - from: "src/lib/engine/decisionEngine.ts"
      to: "src/lib/engine/scoring.ts"
      via: "decision outcomes are driven by named scoring helpers and category-safe driver weights rather than ad hoc hashes"
      pattern: "score|threshold|DecisionState|career|relationships|money|travel|move|communication"
    - from: "src/lib/engine/dailyEngine.ts"
      to: "src/lib/engine/scoring.ts"
      via: "daily and date-aware rendering consume a shared normalized day-seed rule"
      pattern: "normalize|daySeed|dailySeed"
---

<objective>
Build the internal interpretation state and scoring foundation for identity, phase, daily, and decision logic before expanding phrase-bank rendering.

Purpose: establish the clean state pipeline so deterministic rules can be tested independently from phrasing.
Output: typed internal states, named scoring/modifier helpers, category weighting maps, normalized day-seed helpers, and focused engine tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/03-deterministic-interpretation-engines-spec.md
@.planning/phases/03-deterministic-interpretation-engines/03-CONTEXT.md
@.planning/phases/02-sidereal-computation-core/02-REVIEW.md

Relevant source files:
- src/lib/types/engine.ts
- src/lib/engine/identityEngine.ts
- src/lib/engine/phaseEngine.ts
- src/lib/engine/dailyEngine.ts
- src/lib/engine/decisionEngine.ts
- src/lib/engine/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed internal interpretation states</name>
  <files>src/lib/types/engine.ts, src/lib/engine/identityEngine.ts, src/lib/engine/phaseEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts</files>
  <action>
Introduce compact internal state shapes for identity, phase, daily, and decision logic in line with the Phase 3 spec. The engine modules should start deriving these internal states from `ChartPrimitives` before any final copy rendering happens. Keep the state objects compact, explicit, and stable enough for direct testing.

Moon nakshatra should remain the primary identity driver, dasha should remain the primary phase driver, and tara/transit inputs should remain the primary daily-state driver.

Update `src/lib/types/engine.ts` so the exported rendered output types can grow toward the richer Phase 3 product shape during this phase instead of deferring that contract work silently to Phase 4. Make it explicit which richer fields become public engine outputs now.

> verification: test -f src/lib/types/engine.ts
> verification: grep -q 'IdentityState\\|PhaseState\\|DailySignalState\\|DecisionState' src/lib/types/engine.ts
  </action>
  <verify>
test -f src/lib/types/engine.ts
grep -q 'IdentityState\|PhaseState\|DailySignalState\|DecisionState' src/lib/types/engine.ts
  </verify>
  <done>The engine layer exposes typed internal state objects for each interpretation domain.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize thresholds and modifier maps</name>
  <files>src/lib/engine/scoring.ts, src/lib/engine/identityEngine.ts, src/lib/engine/phaseEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts</files>
  <action>
Move interpretation thresholds, score bands, and modifier maps into named helpers or constant structures so the phase does not drift into hidden magic numbers. Keep the scoring deterministic and grounded in chart inputs rather than generic hashes alone.

This task should make it obvious where decision thresholds live, where phase-state mappings are derived, and where daily-state modifiers are classified.

This task must explicitly define category-specific weighting maps and chart-derived driver rules for all six decision categories: `career`, `relationships`, `money`, `travel`, `move`, and `communication`. A hash may still be used as a deterministic tie-breaker or variation helper, but it must not remain the dominant source of meaning.

Define one shared normalized day-seed helper for daily or date-aware rendering so same-day behavior stays stable across engines and next-day changes are intentional.

> verification: test -f src/lib/engine/scoring.ts
> verification: grep -q 'threshold\\|score\\|modifier' src/lib/engine/scoring.ts
> verification: grep -q 'career\\|relationships\\|money\\|travel\\|move\\|communication' src/lib/engine/scoring.ts
> verification: grep -q 'normalize\\|daySeed\\|dailySeed' src/lib/engine/scoring.ts
  </action>
  <verify>
test -f src/lib/engine/scoring.ts
grep -q 'threshold\|score\|modifier' src/lib/engine/scoring.ts
grep -q 'career\|relationships\|money\|travel\|move\|communication' src/lib/engine/scoring.ts
grep -q 'normalize\|daySeed\|dailySeed' src/lib/engine/scoring.ts
  </verify>
  <done>The engine rules are driven by named scoring and modifier structures, explicit category weights, and a shared normalized day-seed rule.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused engine-logic tests</name>
  <files>tests/engine/identityEngine.test.ts, tests/engine/phaseEngine.test.ts, tests/engine/dailyEngine.test.ts, tests/engine/decisionEngine.test.ts</files>
  <action>
Add direct engine tests for the new internal logic layer. At minimum, verify archetype mapping, lagna expression impact, dasha-driven phase mapping, tara/transit daily mapping, and decision threshold behavior across multiple categories. Include one time-unknown case where the logic degrades confidence or falls back explicitly.

Add a direct scoring-level test file that proves category-specific decision weights are chart-derived, same-day normalized seeds stay stable, and next-day normalized seeds can change when intended.

> verification: test -f tests/engine/identityEngine.test.ts
> verification: test -f tests/engine/decisionEngine.test.ts
> verification: test -f tests/engine/scoring.test.ts
> verification: grep -q 'describe' tests/engine/decisionEngine.test.ts
  </action>
  <verify>
test -f tests/engine/identityEngine.test.ts
test -f tests/engine/decisionEngine.test.ts
test -f tests/engine/scoring.test.ts
grep -q 'describe' tests/engine/decisionEngine.test.ts
  </verify>
  <done>The interpretation logic layer is protected by direct deterministic engine tests and scoring-level seed verification.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Typed internal states exist for identity, phase, daily, and decision logic.
- [ ] Thresholds and modifier maps are named and reusable.
- [ ] Decision scoring exposes category-specific chart-derived weights.
- [ ] A shared normalized day-seed rule exists for daily and date-aware rendering.
- [ ] Engine tests protect core deterministic mappings, fallback behavior, and seed stability.
</verification>

<success_criteria>
- The clean state pipeline exists in code, not just in the spec.
- Phrase rendering can evolve later without reshaping core logic again.
- Interpretation rules are testable independently from copy selection.
- Decision logic becomes more chart-meaningful instead of staying mostly hash-shaped behind cleaner naming.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-interpretation-engines/03-01-SUMMARY.md`

Summarize the internal state model, the named scoring structures, the category weighting model, the normalized day-seed rule, and the logic tests that now protect them.
</output>
