---
phase: 03-deterministic-interpretation-engines
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
  - src/lib/engine/scoring.ts
  - src/lib/types/engine.ts
  - tests/engine/identityEngine.test.ts
  - tests/engine/phaseEngine.test.ts
  - tests/engine/dailyEngine.test.ts
  - tests/engine/decisionEngine.test.ts
  - tests/engine/phraseSelection.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - src/lib/astro/adapter.ts
  - src/lib/schemas/input.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/engine/identityEngine.ts"
    provides: "Integrated Moon-led identity engine with time-unknown fallback behavior"
    required: true
  - path: "src/lib/engine/dailyEngine.ts"
    provides: "Integrated daily-state engine driven by tara/transit inputs"
    required: true
  - path: "src/lib/types/engine.ts"
    provides: "Final Phase 3 engine output contract aligned to richer rendered behavior and Phase 4 handoff"
    required: true
  - path: "tests/engine/dailyEngine.test.ts"
    provides: "Daily engine verification for exact-time and time-unknown behavior"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-qa-verification-specialist]
requirements: [R4, R5, R6, R7, R8]
user_setup: []
verification_commands:
  - "test -f src/lib/engine/identityEngine.ts"
  - "test -f src/lib/engine/dailyEngine.ts"
  - "test -f src/lib/types/engine.ts"
  - "test -f tests/engine/dailyEngine.test.ts"
  - "grep -q 'time-unknown\\|confidence' src/lib/engine/dailyEngine.ts"
  - "grep -q 'normalize\\|daySeed\\|dailySeed' src/lib/engine/dailyEngine.ts"
  - "grep -q 'summary\\|guidance\\|rationale\\|watchouts\\|supportAction\\|cautionAction' src/lib/types/engine.ts"
must_haves:
  truths:
    - "The engines consume the real Phase 2 chart contract without reshaping it again."
    - "Moon-led fallback behavior remains strong for time-unknown mode."
    - "No implicit instant-level time drift is reintroduced through engine seeds or renderer selection."
    - "The richer rendered engine output contract is finalized in Phase 3 so Phase 4 can wire routes without another hidden response redesign."
  artifacts:
    - path: "src/lib/engine/identityEngine.ts"
      provides: "Integrated identity pipeline"
      min_lines: 20
      contains: "moon|lagna|confidence"
    - path: "src/lib/engine/dailyEngine.ts"
      provides: "Integrated daily-state pipeline"
      min_lines: 18
      contains: "tara|transit|confidence"
    - path: "tests/engine/dailyEngine.test.ts"
      provides: "Daily-state behavior verification"
      min_lines: 12
      contains: "describe"
    - path: "src/lib/types/engine.ts"
      provides: "Final rendered engine output contract"
      min_lines: 55
      contains: "summary|guidance|rationale|watchouts|supportAction|cautionAction"
  key_links:
    - from: "src/lib/engine/phaseEngine.ts"
      to: "src/lib/types/astrology.ts"
      via: "phase derivation consumes real dasha state from the chart contract"
      pattern: "dasha|mahadasha|antardasha"
    - from: "src/lib/engine/dailyEngine.ts"
      to: "src/lib/types/astrology.ts"
      via: "daily derivation consumes tara and transit state from Phase 2 outputs"
      pattern: "tara|transit|confidence"
    - from: "src/lib/engine/dailyEngine.ts"
      to: "src/lib/types/engine.ts"
      via: "daily rendering and confidence behavior align with the exported Phase 3 output contract"
      pattern: "guidance|caution|confidence"
---

<objective>
Integrate the clean state pipeline with the real Phase 2 chart contract and lock the engine behavior for exact-time and time-unknown inputs.

Purpose: finish Phase 3 by turning the internal state model and expanded phrase banks into stable deterministic engine outputs.
Output: integrated engines, finalized rendered output-contract alignment, and integration-style engine tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/03-deterministic-interpretation-engines-spec.md
@.planning/phases/03-deterministic-interpretation-engines/03-CONTEXT.md
@.planning/phases/03-deterministic-interpretation-engines/03-01-SUMMARY.md
@.planning/phases/03-deterministic-interpretation-engines/03-02-SUMMARY.md
@.planning/phases/02-sidereal-computation-core/02-REVIEW.md

Relevant source files:
- src/lib/types/engine.ts
- src/lib/types/astrology.ts
- src/lib/engine/identityEngine.ts
- src/lib/engine/phaseEngine.ts
- src/lib/engine/dailyEngine.ts
- src/lib/engine/decisionEngine.ts
- src/lib/engine/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate the engines against the real chart primitive contract</name>
  <files>src/lib/engine/identityEngine.ts, src/lib/engine/phaseEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts, src/lib/engine/scoring.ts</files>
  <action>
Refactor the engine implementations so they consume the real Phase 2 chart contract cleanly. Identity must stay Moon-led with Lagna as an expression modifier. Phase must stay dasha-led. Daily must stay tara/transit-led. Decision logic must stay category-limited and deterministic.

Do not reintroduce implicit instant-level seeds or hidden runtime entropy. If any seed depends on time, it must be normalized to a stable day boundary or to an explicit provided seed.

This task must prove that the category weighting rules introduced in Wave 1 actually drive decision outcomes in the integrated engine path, rather than letting hash-shaped variation dominate the final result.

> verification: test -f src/lib/engine/identityEngine.ts
> verification: test -f src/lib/engine/dailyEngine.ts
  </action>
  <verify>
test -f src/lib/engine/identityEngine.ts
test -f src/lib/engine/dailyEngine.ts
  </verify>
  <done>The engine layer consumes the real chart primitives cleanly, deterministically, and through chart-meaningful decision rules.</done>
</task>

<task type="auto">
  <name>Task 2: Lock exact-time and time-unknown fallback behavior</name>
  <files>src/lib/engine/identityEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts, src/lib/types/engine.ts</files>
  <action>
Make the fallback behavior explicit for time-unknown mode. Identity must still work strongly from Moon-led inputs, daily and decision outputs must degrade confidence intentionally rather than vaguely, and any missing Lagna-dependent logic must be handled without crashing or pretending exact certainty.

Finalize `src/lib/types/engine.ts` during this task so the richer Phase 3 rendered field structure is explicit before Phase 4 API delivery begins. Treat this as the handoff contract for later route wiring.

> verification: grep -q 'time-unknown\\|confidence' src/lib/engine/dailyEngine.ts
> verification: grep -q 'lagna\\|moon' src/lib/engine/identityEngine.ts
> verification: grep -q 'summary\\|guidance\\|rationale\\|watchouts\\|supportAction\\|cautionAction' src/lib/types/engine.ts
  </action>
  <verify>
grep -q 'time-unknown\|confidence' src/lib/engine/dailyEngine.ts
grep -q 'lagna\|moon' src/lib/engine/identityEngine.ts
grep -q 'summary\|guidance\|rationale\|watchouts\|supportAction\|cautionAction' src/lib/types/engine.ts
  </verify>
  <done>The engines model exact-time and time-unknown behavior explicitly and finalize the richer rendered output contract for Phase 4 handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration-style engine verification</name>
  <files>tests/engine/identityEngine.test.ts, tests/engine/phaseEngine.test.ts, tests/engine/dailyEngine.test.ts, tests/engine/decisionEngine.test.ts, tests/engine/phraseSelection.test.ts</files>
  <action>
Tighten the tests so the engine layer is protected end to end. Verify exact-time and time-unknown outputs, deterministic phrase rendering, and category-safe decision behavior. Keep assertions explicit and stable rather than relying on snapshots.

The verification must prove same-day stability for normalized daily behavior, intended next-day variation where applicable, and state-to-renderer wiring across identity, phase, daily, and decision flows.

> verification: test -f tests/engine/dailyEngine.test.ts
> verification: grep -q 'time-unknown\\|confidence' tests/engine/dailyEngine.test.ts
  </action>
  <verify>
test -f tests/engine/dailyEngine.test.ts
grep -q 'time-unknown\|confidence' tests/engine/dailyEngine.test.ts
  </verify>
  <done>The Phase 3 engines are protected by integration-style deterministic tests across the main interpretation flows.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Engines consume the real Phase 2 chart contract without reshaping it.
- [ ] Time-unknown fallback behavior is explicit and Moon-led where appropriate.
- [ ] The richer rendered engine output contract is finalized in `src/lib/types/engine.ts`.
- [ ] Engine tests cover exact-time and time-unknown behavior, decision thresholds, same-day stability, next-day variation, and phrase determinism.
</verification>

<success_criteria>
- Phase 3 ends with deterministic engines, not placeholder renderers.
- The engine layer is stable enough for Phase 4 API delivery without another major refactor.
- Time-unknown mode remains a supported product path rather than a degraded afterthought.
- Phase 4 can wire the engine outputs directly instead of rediscovering the response contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-interpretation-engines/03-03-SUMMARY.md`

Summarize the final engine integration, the exact-time versus time-unknown behavior, the Phase 4 handoff contract, and the integration-style tests that now protect the interpretation layer.
</output>
