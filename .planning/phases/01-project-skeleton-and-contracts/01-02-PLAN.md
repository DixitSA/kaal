---
phase: 01-project-skeleton-and-contracts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/api/chart/compute/route.ts
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - src/app/api/today/[userId]/route.ts
  - src/lib/astro/adapter.ts
  - src/lib/astro/calculateJulianDay.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/astro/calculateNakshatra.ts
  - src/lib/astro/calculateDasha.ts
  - src/lib/astro/calculateTransits.ts
  - src/lib/astro/calculateTaraBala.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
  - src/lib/engine/scoring.ts
  - src/lib/content/archetypePhrases.ts
  - src/lib/content/phasePhrases.ts
  - src/lib/content/dailyPhrases.ts
  - src/lib/content/decisionPhrases.ts
  - tests/api/profile-generate.test.ts
  - tests/api/decision-evaluate.test.ts
  - tests/api/validation.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - package.json
  - tsconfig.json
  - src/lib/types/astrology.ts
  - src/lib/types/engine.ts
  - src/lib/types/api.ts
  - src/lib/schemas/input.ts
  - src/lib/schemas/output.ts
sequential_files: []
expected_artifacts:
  - path: "src/app/api/profile/generate/route.ts"
    provides: "Profile-generation route skeleton wired to schemas and placeholder engine seams"
    required: true
  - path: "src/lib/astro/adapter.ts"
    provides: "Astrology adapter seam reserved for the future swisseph integration"
    required: true
  - path: "src/lib/engine/identityEngine.ts"
    provides: "Typed interpretation-engine placeholder module for later implementation"
    required: true
  - path: "tests/api/validation.test.ts"
    provides: "API-level validation smoke tests for the route skeleton"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-api-tester]
requirements: [R8, R9, R10]
user_setup: []
verification_commands:
  - "test -f src/app/api/profile/generate/route.ts"
  - "test -f src/lib/astro/adapter.ts"
  - "test -f tests/api/validation.test.ts"
  - "grep -q 'birthInputSchema' src/app/api/profile/generate/route.ts"

must_haves:
  truths:
    - "All four planned route handlers exist and validate input before handing off to placeholder implementation seams."
    - "Placeholder route responses use the shared success/error schemas defined in Wave 1 instead of inventing ad hoc JSON bodies."
    - "Placeholder astro, engine, and content modules preserve the requested file layout so later phases can replace stubs without changing import paths."
    - "API smoke tests prove invalid requests fail cleanly and that the placeholder contract surface stays deterministic and database-free."
  artifacts:
    - path: "src/app/api/profile/generate/route.ts"
      provides: "Profile route skeleton"
      min_lines: 20
      contains: "birthInputSchema"
    - path: "src/lib/astro/adapter.ts"
      provides: "Astrology adapter seam"
      min_lines: 12
      contains: "adapter"
    - path: "tests/api/validation.test.ts"
      provides: "Validation smoke tests"
      min_lines: 15
      contains: "describe"
  key_links:
    - from: "src/app/api/profile/generate/route.ts"
      to: "src/lib/schemas/input.ts"
      via: "route-level request parsing reuses shared Zod schemas"
      pattern: "birthInputSchema"
    - from: "src/app/api/decision/evaluate/route.ts"
      to: "src/lib/engine/decisionEngine.ts"
      via: "decision route hands execution off through a dedicated engine seam"
      pattern: "decision"
---

<objective>
Create the API-layer scaffold and the placeholder astro/engine/content modules that make the Kaal backend executable as a structured skeleton before the real domain logic lands. This plan should leave Phase 1 with validated route-handler entry points and a smoke-test harness.

Purpose: turn the Wave 1 contracts into concrete route and module seams that later phases can implement without reshaping the public API.
Output: route-handler skeletons, placeholder domain modules, and API validation tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-skeleton-and-contracts/01-CONTEXT.md
@.planning/phases/01-project-skeleton-and-contracts/01-01-SUMMARY.md

Relevant source files:
- src/lib/types/astrology.ts
- src/lib/types/engine.ts
- src/lib/types/api.ts
- src/lib/schemas/input.ts
- src/lib/schemas/output.ts
- src/lib/utils/deterministicPick.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder astro, engine, and content modules</name>
  <files>src/lib/astro/adapter.ts, src/lib/astro/calculateJulianDay.ts, src/lib/astro/calculateChart.ts, src/lib/astro/calculateNakshatra.ts, src/lib/astro/calculateDasha.ts, src/lib/astro/calculateTransits.ts, src/lib/astro/calculateTaraBala.ts, src/lib/engine/identityEngine.ts, src/lib/engine/phaseEngine.ts, src/lib/engine/dailyEngine.ts, src/lib/engine/decisionEngine.ts, src/lib/engine/scoring.ts, src/lib/content/archetypePhrases.ts, src/lib/content/phasePhrases.ts, src/lib/content/dailyPhrases.ts, src/lib/content/decisionPhrases.ts</files>
  <action>
Create the requested domain modules at their final paths, but keep them as typed, deterministic placeholders for now. The goal in this phase is to lock in the import boundaries and exported signatures, not to implement full astrology computation or interpretation logic yet. `src/lib/astro/adapter.ts` must clearly establish the seam where `swisseph` will eventually be wrapped, while the calculation modules export placeholder functions or TODO-safe stubs that do not hide future work.

Do the same for the interpretation and content layers: each engine module should expose a typed function boundary aligned with the project brief, and each phrase-bank file should export deterministic placeholder data structures ready for future expansion. Keep the stubs honest: either return clearly marked placeholder deterministic values or explicit not-yet-implemented responses that the route layer can handle predictably.

> verification: test -f src/lib/astro/adapter.ts
> verification: test -f src/lib/engine/identityEngine.ts
> verification: test -f src/lib/content/decisionPhrases.ts
> verification: grep -q 'swisseph' src/lib/astro/adapter.ts
  </action>
  <verify>
test -f src/lib/astro/adapter.ts
test -f src/lib/engine/identityEngine.ts
test -f src/lib/content/decisionPhrases.ts
grep -q 'swisseph' src/lib/astro/adapter.ts
  </verify>
  <done>The requested astro, engine, and content module files exist with typed placeholder exports and stable import seams.</done>
</task>

<task type="auto">
  <name>Task 2: Add validated API route-handler skeletons</name>
  <files>src/app/api/chart/compute/route.ts, src/app/api/profile/generate/route.ts, src/app/api/decision/evaluate/route.ts, src/app/api/today/[userId]/route.ts</files>
  <action>
Create the four App Router route handlers at the exact file paths from the project brief. Each route should parse and validate its inputs with the shared Zod schemas from Wave 1 before invoking the appropriate placeholder astro or engine seam. Keep the handlers small and typed. For routes whose real logic is deferred to later phases, return a stable JSON placeholder or explicit not-implemented contract that is still deterministic and easy to test.

`/api/chart/compute`, `/api/profile/generate`, and `/api/decision/evaluate` must demonstrate the request/response contract boundaries and 400-level error handling for invalid input. `/api/today/[userId]` should be a clean placeholder that makes the later persistence seam obvious without forcing a database into v1.

Do not invent route-specific placeholder envelopes in this plan. Import and use the shared response and error schemas from Wave 1. For `/api/today/[userId]`, keep the route stateless and database-free: treat `userId` according to the semantics frozen in Wave 1 and return the exact deterministic placeholder shape defined there. Also make malformed JSON, invalid params, and validation failures map to a consistent error envelope instead of only handling the happy path.

> verification: test -f src/app/api/chart/compute/route.ts
> verification: test -f src/app/api/profile/generate/route.ts
> verification: test -f src/app/api/decision/evaluate/route.ts
> verification: grep -q 'birthInputSchema' src/app/api/profile/generate/route.ts
> verification: grep -q 'errorResponseSchema' src/app/api/profile/generate/route.ts
  </action>
  <verify>
test -f src/app/api/chart/compute/route.ts
test -f src/app/api/profile/generate/route.ts
test -f src/app/api/decision/evaluate/route.ts
grep -q 'birthInputSchema' src/app/api/profile/generate/route.ts
grep -q 'errorResponseSchema' src/app/api/profile/generate/route.ts
  </verify>
  <done>The planned route handlers exist, validate input with shared schemas, and hand off to deterministic placeholder logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add API smoke tests for validation and placeholder success paths</name>
  <files>tests/api/profile-generate.test.ts, tests/api/decision-evaluate.test.ts, tests/api/validation.test.ts</files>
  <action>
Create a lightweight API test harness focused on Phase 1 guarantees: invalid inputs return clear 400 responses, and the route skeletons return stable placeholder success contracts when provided with valid inputs. Keep the tests implementation-aware enough to protect the route boundaries without forcing the full Phase 2 and Phase 3 engines to exist yet.

At minimum, cover:
- invalid birth input handling
- successful profile generation skeleton flow
- successful decision evaluation skeleton flow
- deterministic placeholder contract behavior for `/api/today/[userId]`
- consistent error envelope behavior for validation and placeholder/not-implemented responses

Use the testing setup created in Wave 1 and keep the tests local-first with no database or external service dependency.

> verification: test -f tests/api/validation.test.ts
> verification: test -f tests/api/profile-generate.test.ts
> verification: grep -q 'describe' tests/api/validation.test.ts
> verification: grep -q 'decision' tests/api/decision-evaluate.test.ts
> verification: grep -q 'today' tests/api/validation.test.ts
  </action>
  <verify>
test -f tests/api/validation.test.ts
test -f tests/api/profile-generate.test.ts
grep -q 'describe' tests/api/validation.test.ts
grep -q 'decision' tests/api/decision-evaluate.test.ts
grep -q 'today' tests/api/validation.test.ts
  </verify>
  <done>The API scaffold has smoke tests that protect the validation paths and placeholder response contracts for the current phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The requested route-handler file paths exist under `src/app/api`.
- [ ] Placeholder astro, engine, and content modules exist at the final import locations for later phases.
- [ ] API smoke tests cover invalid input, consistent error envelopes, and placeholder success paths without requiring a database.
</verification>

<success_criteria>
- Phase 1 ends with an executable backend scaffold rather than only documents and types.
- Future phases can implement real astrology logic by replacing placeholder module internals instead of moving files or changing route contracts.
- The project now has a basic automated safety net around the API boundaries and placeholder contract shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton-and-contracts/01-02-SUMMARY.md`

Summarize the route-handler scaffold, list the placeholder module seams that were established, and record any deferred implementation notes that later phases must honor.
</output>
