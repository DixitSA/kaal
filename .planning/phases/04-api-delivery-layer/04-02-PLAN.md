---
phase: 04-api-delivery-layer
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/api/today/[userId]/route.ts
  - src/lib/types/api.ts
  - src/lib/schemas/output.ts
  - tests/api/validation.test.ts
  - tests/api/today-route.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/lib/astro/adapter.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
  - src/app/api/chart/compute/route.ts
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
sequential_files: []
expected_artifacts:
  - path: "src/app/api/today/[userId]/route.ts"
    provides: "Explicitly stateless today-route placeholder with a clear persistence seam"
    required: true
  - path: "tests/api/today-route.test.ts"
    provides: "Focused today-route verification"
    required: true
  - path: "tests/api/validation.test.ts"
    provides: "Cross-route validation and deterministic API verification"
    required: true
autonomous: false
agents: [engineering-senior-developer, testing-api-tester]
requirements: [R8, R9, R10, R12]
user_setup: []
verification_commands:
  - "test -f src/app/api/today/[userId]/route.ts"
  - "test -f tests/api/today-route.test.ts"
  - "grep -q 'stateless-placeholder' src/app/api/today/[userId]/route.ts"
  - "grep -q 'deterministic\\|today' tests/api/today-route.test.ts"
  - "grep -q 'time-unknown\\|VALIDATION_ERROR' tests/api/validation.test.ts"
must_haves:
  truths:
    - "The today route stays explicitly stateless and database-free in v1."
    - "The today placeholder mode remains visible instead of implying persistence that does not exist."
    - "Route-level verification, not just engine tests, becomes the primary proof for the final API surface."
    - "Phase 4 finishes with a stable route layer that Phase 5 can verify without another contract reshape."
  artifacts:
    - path: "src/app/api/today/[userId]/route.ts"
      provides: "Stateless today-route delivery seam"
      min_lines: 20
      contains: "stateless-placeholder|generatedAt|daily|userId"
    - path: "tests/api/today-route.test.ts"
      provides: "Focused today-route coverage"
      min_lines: 15
      contains: "describe"
    - path: "tests/api/validation.test.ts"
      provides: "Cross-route API verification"
      min_lines: 25
      contains: "chart/compute|today|VALIDATION_ERROR"
  key_links:
    - from: "src/app/api/today/[userId]/route.ts"
      to: "src/lib/types/api.ts"
      via: "today-route behavior matches the declared placeholder contract and remains persistence-ready"
      pattern: "TodaySuccessData|mode|userId|daily"
    - from: "tests/api/today-route.test.ts"
      to: "src/app/api/today/[userId]/route.ts"
      via: "today-route determinism and placeholder semantics are protected at the route layer"
      pattern: "stateless|deterministic|userId"
---

<objective>
Finalize the stateless today-route placeholder behavior and lock the full Phase 4 API surface with route-focused verification.

Purpose: finish the route layer so it stays local-first, deterministic, and ready for later persistence without implying a database now.
Output: explicit today-route semantics and a stronger route-level verification matrix.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/04-api-delivery-layer-spec.md
@.planning/phases/04-api-delivery-layer/04-CONTEXT.md
@.planning/phases/04-api-delivery-layer/04-01-SUMMARY.md
@.planning/phases/03-deterministic-interpretation-engines/03-REVIEW.md

Relevant source files:
- src/app/api/today/[userId]/route.ts
- src/lib/types/api.ts
- src/lib/schemas/output.ts
- tests/api/validation.test.ts

Agent: engineering-senior-developer
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the today route explicitly stateless and persistence-ready</name>
  <files>src/app/api/today/[userId]/route.ts, src/lib/types/api.ts, src/lib/schemas/output.ts</files>
  <action>
Refine the today route so its placeholder behavior is explicit, deterministic, and honest about the fact that v1 has no persistence. Keep the placeholder mode visible, keep the route database-free, and make the later persistence seam obvious in structure without forcing that architecture now.

This task may align the shared API type or output schema if the placeholder semantics need to be clarified, but it must not redesign the route contract away from the local-first v1 plan.

> verification: test -f src/app/api/today/[userId]/route.ts
> verification: grep -q 'stateless-placeholder' src/app/api/today/[userId]/route.ts
  </action>
  <verify>
test -f src/app/api/today/[userId]/route.ts
grep -q 'stateless-placeholder' src/app/api/today/[userId]/route.ts
  </verify>
  <done>The today route is explicitly stateless, deterministic, and ready for future persistence without implying it already exists.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused today-route tests and complete the API verification matrix</name>
  <files>tests/api/today-route.test.ts, tests/api/validation.test.ts</files>
  <action>
Add focused tests for the today route covering deterministic behavior, invalid params, and the explicit placeholder contract. Then strengthen the shared API verification matrix so the final Phase 4 proof is route-centered, including malformed input, validation failures, success envelopes, and time-unknown compatibility where relevant.

Prefer direct assertions over snapshots. Make the tests readable enough to serve as the Phase 4 route contract proof.

> verification: test -f tests/api/today-route.test.ts
> verification: grep -q 'describe' tests/api/today-route.test.ts
> verification: grep -q 'time-unknown\\|VALIDATION_ERROR' tests/api/validation.test.ts
  </action>
  <verify>
test -f tests/api/today-route.test.ts
grep -q 'describe' tests/api/today-route.test.ts
grep -q 'time-unknown\|VALIDATION_ERROR' tests/api/validation.test.ts
  </verify>
  <done>The final API surface is protected by explicit route-level tests, including focused coverage for the today route.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm Phase 5 handoff stability</name>
  <files>tests/api/validation.test.ts, tests/api/today-route.test.ts</files>
  <action>
Use the final route-focused verification layer to prove Phase 5 can concentrate on broad verification and extension readiness rather than rediscovering route contract drift. Make sure the test suite clearly covers stable envelopes, deterministic today behavior, and local-first assumptions.

If any Phase 4 contract nuance needs to be called out for Phase 5, make that explicit in the plan summary rather than burying it in code.

> verification: grep -q 'deterministic\\|today' tests/api/today-route.test.ts
> verification: grep -q 'chart/compute\\|profile/generate\\|decision/evaluate\\|today' tests/api/validation.test.ts
  </action>
  <verify>
grep -q 'deterministic\|today' tests/api/today-route.test.ts
grep -q 'chart/compute\|profile/generate\|decision/evaluate\|today' tests/api/validation.test.ts
  </verify>
  <done>Phase 4 ends with a route layer that Phase 5 can verify without another delivery-contract redesign.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The today route remains explicit, deterministic, and stateless.
- [ ] The placeholder mode stays visible in the public contract.
- [ ] Route-level tests cover invalid params, deterministic today behavior, and cross-route envelope consistency.
- [ ] The final API layer stays local-first and database-free.
</verification>

<success_criteria>
- `/api/today/[userId]` is clearly a v1 placeholder seam, not an accidental pseudo-persistence layer.
- Phase 4 finishes with a route surface that is thin, validated, and ready for broad verification in Phase 5.
- The backend remains local-first with no hidden database dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-delivery-layer/04-02-SUMMARY.md`

Summarize the today-route placeholder semantics, the final API verification matrix, and any explicit handoff notes for Phase 5 verification and extension readiness.
</output>
