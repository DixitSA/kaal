---
phase: 04-api-delivery-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/api.ts
  - src/lib/schemas/input.ts
  - src/lib/schemas/output.ts
  - src/lib/api/routeHelpers.ts
  - src/app/api/chart/compute/route.ts
  - src/app/api/profile/generate/route.ts
  - src/app/api/decision/evaluate/route.ts
  - tests/api/validation.test.ts
  - tests/api/profile-generate.test.ts
  - tests/api/decision-evaluate.test.ts
files_forbidden:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - src/app/api/today/[userId]/route.ts
  - src/lib/astro/adapter.ts
  - src/lib/astro/calculateChart.ts
  - src/lib/engine/identityEngine.ts
  - src/lib/engine/phaseEngine.ts
  - src/lib/engine/dailyEngine.ts
  - src/lib/engine/decisionEngine.ts
sequential_files: []
expected_artifacts:
  - path: "src/lib/api/routeHelpers.ts"
    provides: "Thin shared helpers for JSON parsing and consistent validation-error delivery"
    required: true
  - path: "src/lib/types/api.ts"
    provides: "Aligned TypeScript API route contracts"
    required: true
  - path: "src/lib/schemas/output.ts"
    provides: "Shared runtime response schemas aligned with actual route outputs"
    required: true
  - path: "tests/api/profile-generate.test.ts"
    provides: "Profile route contract verification"
    required: true
autonomous: false
agents: [engineering-backend-architect, testing-api-tester]
requirements: [R8, R9, R10]
user_setup: []
verification_commands:
  - "test -f src/lib/api/routeHelpers.ts"
  - "test -f src/lib/types/api.ts"
  - "test -f src/lib/schemas/output.ts"
  - "grep -q 'VALIDATION_ERROR' src/lib/api/routeHelpers.ts"
  - "grep -q '/api/chart/compute\\|/api/profile/generate\\|/api/decision/evaluate' src/lib/types/api.ts"
  - "grep -q 'contractVersion' src/lib/schemas/output.ts"
must_haves:
  truths:
    - "The three POST routes share consistent parse, validation, and success-envelope behavior."
    - "Runtime schemas and TypeScript API contracts stay aligned with the actual returned payloads."
    - "The richer Phase 3 profile and decision fields remain visible at the API boundary instead of being narrowed away."
    - "Route helpers, if introduced, remain delivery-focused and do not absorb astro or engine business logic."
  artifacts:
    - path: "src/lib/api/routeHelpers.ts"
      provides: "Thin delivery helper boundary"
      min_lines: 20
      contains: "VALIDATION_ERROR|Request body must be valid JSON|Response.json"
    - path: "src/lib/types/api.ts"
      provides: "API route contract types aligned to runtime"
      min_lines: 40
      contains: "/api/chart/compute|/api/profile/generate|/api/decision/evaluate|/api/today/[userId]"
    - path: "src/lib/schemas/output.ts"
      provides: "Shared output validation boundary"
      min_lines: 40
      contains: "contractVersion|identity|phase|decision|daily"
    - path: "tests/api/profile-generate.test.ts"
      provides: "Profile route verification"
      min_lines: 15
      contains: "profileResponseSchema|contractVersion"
  key_links:
    - from: "src/app/api/profile/generate/route.ts"
      to: "src/lib/schemas/output.ts"
      via: "route success payload is validated by the shared runtime schema rather than a route-local shape"
      pattern: "profileResponseSchema"
    - from: "src/app/api/decision/evaluate/route.ts"
      to: "src/lib/types/api.ts"
      via: "route behavior matches the declared TypeScript API contract"
      pattern: "DecisionEvaluate"
    - from: "src/app/api/chart/compute/route.ts"
      to: "src/lib/api/routeHelpers.ts"
      via: "route parse and validation behavior use the shared thin delivery boundary"
      pattern: "parse|validation|json"
---

<objective>
Align the shared API delivery contracts and standardize thin route helpers before finalizing the full Phase 4 route surface.

Purpose: remove route-layer drift and keep delivery behavior consistent across the three POST handlers.
Output: aligned route contracts, thin shared delivery helpers, and POST route verification.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/specs/04-api-delivery-layer-spec.md
@.planning/phases/04-api-delivery-layer/04-CONTEXT.md
@.planning/phases/03-deterministic-interpretation-engines/03-REVIEW.md

Relevant source files:
- src/lib/types/api.ts
- src/lib/schemas/input.ts
- src/lib/schemas/output.ts
- src/app/api/chart/compute/route.ts
- src/app/api/profile/generate/route.ts
- src/app/api/decision/evaluate/route.ts
- tests/api/validation.test.ts
- tests/api/profile-generate.test.ts
- tests/api/decision-evaluate.test.ts

Agent: engineering-backend-architect
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the shared route contract surface</name>
  <files>src/lib/types/api.ts, src/lib/schemas/input.ts, src/lib/schemas/output.ts</files>
  <action>
Reconcile the shared TypeScript and Zod route contracts so the route layer is contract-honest across request bodies, response envelopes, and the actual richer Phase 3 payload shapes. Keep the runtime schemas authoritative and make sure `src/lib/types/api.ts` is not drifting into a different public shape.

Do not redesign engine or astro output here. This is route delivery alignment only.

> verification: test -f src/lib/types/api.ts
> verification: test -f src/lib/schemas/output.ts
> verification: grep -q 'contractVersion' src/lib/schemas/output.ts
  </action>
  <verify>
test -f src/lib/types/api.ts
test -f src/lib/schemas/output.ts
grep -q 'contractVersion' src/lib/schemas/output.ts
  </verify>
  <done>The shared API types and runtime schemas describe the same public contract.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize thin delivery helpers and refactor the POST routes</name>
  <files>src/lib/api/routeHelpers.ts, src/app/api/chart/compute/route.ts, src/app/api/profile/generate/route.ts, src/app/api/decision/evaluate/route.ts</files>
  <action>
Extract or standardize a thin delivery helper boundary for repeated route concerns such as malformed-JSON handling and shared validation-error shaping. Then refactor the three POST routes to use the clean delivery boundary without moving business logic out of the adapter or engine layers.

The routes should remain small orchestrators: parse, validate, invoke, and return.

> verification: test -f src/lib/api/routeHelpers.ts
> verification: grep -q 'VALIDATION_ERROR' src/lib/api/routeHelpers.ts
> verification: grep -q 'profileResponseSchema' src/app/api/profile/generate/route.ts
  </action>
  <verify>
test -f src/lib/api/routeHelpers.ts
grep -q 'VALIDATION_ERROR' src/lib/api/routeHelpers.ts
grep -q 'profileResponseSchema' src/app/api/profile/generate/route.ts
  </verify>
  <done>The three POST routes share consistent delivery behavior through a thin helper boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten POST-route contract tests</name>
  <files>tests/api/validation.test.ts, tests/api/profile-generate.test.ts, tests/api/decision-evaluate.test.ts</files>
  <action>
Add or tighten route-level tests for malformed JSON, validation failures, success envelopes, and the richer Phase 3 profile and decision payload fields. The purpose is to prove the delivery layer preserves the public contract instead of only proving that the engines work underneath.

Prefer explicit field assertions over loose snapshots.

> verification: test -f tests/api/profile-generate.test.ts
> verification: test -f tests/api/decision-evaluate.test.ts
> verification: grep -q 'VALIDATION_ERROR\\|Request body must be valid JSON' tests/api/validation.test.ts
  </action>
  <verify>
test -f tests/api/profile-generate.test.ts
test -f tests/api/decision-evaluate.test.ts
grep -q 'VALIDATION_ERROR\|Request body must be valid JSON' tests/api/validation.test.ts
  </verify>
  <done>The three POST routes are protected by route-focused contract and failure-path tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Shared route types and runtime schemas are aligned.
- [ ] The three POST routes use consistent parse and validation behavior.
- [ ] The richer Phase 3 fields are preserved at the API boundary.
- [ ] Route helpers remain thin and delivery-only.
- [ ] Route tests prove malformed JSON, validation failures, and success envelopes.
</verification>

<success_criteria>
- The POST routes are delivery-consistent instead of independently drifting.
- The API contract is honest in both TypeScript and runtime validation.
- Phase 4 establishes a clean route boundary without reabsorbing business logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-delivery-layer/04-01-SUMMARY.md`

Summarize the shared delivery helper boundary, the contract-alignment work across types and runtime schemas, and the POST-route verification that now protects the API layer.
</output>
